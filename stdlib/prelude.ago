# Ago Standard Library Prelude
# These functions are automatically available in all Ago programs.

# longitudoa - Returns the length of any list as an int
# Works with all list types: -aem, -arum, -as, -erum, -uum
# Casting a list to int (.a()) gives its length
des longitudoa(listuum) {
    redeo listuum.a()
}
